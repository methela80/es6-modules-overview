const add = (first, second) =>{
    return first + second;
}
const multiply = (first, second) =>{

    return first * second;
}

const divideTheFirstNumberByTheSecondNumber = () =>{
    return first / second;
}

export {add, multiply};